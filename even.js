const isEven = function (num) {
  return num % 2 === 0;
}

// In this case we used variables to store the returned value and then we printed it. but it is not necessary as the code below proves that we can just use the returned value;
/* const tenIsEven = isEven(10);
const elevenIsEven = isEven(11);

console.log(tenIsEven);
console.log(elevenIsEven);  */

console.log(isEven(10)); 
console.log(isEven(11)); 